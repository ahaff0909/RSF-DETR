# RSF-DETR CERB+MESA ablation (VFL).

nc: 5
scales:
  l: [1.00, 1.00, 1024]

backbone:
  - [-1, 1, ConvNormLayer, [32, 3, 2, None, False, 'relu']] # 0
  - [-1, 1, ConvNormLayer, [32, 3, 1, None, False, 'relu']] # 1
  - [-1, 1, ConvNormLayer, [64, 3, 1, None, False, 'relu']] # 2
  - [-1, 1, nn.MaxPool2d, [3, 2, 1]] # 3

  - [-1, 1, Blocks, [64, BasicBlock, 2, 2, 'relu']] # 4
  - [-1, 1, Blocks, [128, BasicBlock, 2, 3, 'relu']] # 5-P3/8
  - [-1, 1, CERB, [128, 4, 2]] # 6
  - [-1, 1, Blocks, [256, BasicBlock, 2, 4, 'relu']] # 7-P4/16
  - [-1, 1, CERB, [256, 8, 2]] # 8
  - [-1, 1, Blocks, [512, BasicBlock, 2, 5, 'relu']] # 9-P5/32
  - [-1, 1, CERB, [512, 16, 2]] # 10

head:
  - [10, 1, MESA, [512]] # 11
  - [8, 1, MESA, [256]] # 12
  - [6, 1, MESA, [128]] # 13

  - [11, 1, Conv, [256, 1, 1, None, 1, 1, False]] # 14
  - [-1, 1, AIFI, [1024, 8]] # 15
  - [-1, 1, Conv, [256, 1, 1]] # 16

  - [-1, 1, nn.Upsample, [None, 2, 'nearest']] # 17
  - [12, 1, Conv, [256, 1, 1, None, 1, 1, False]] # 18
  - [[-2, -1], 1, Concat, [1]] # 19
  - [-1, 3, RepC3, [256, 0.5]] # 20
  - [-1, 1, Conv, [256, 1, 1]] # 21

  - [-1, 1, nn.Upsample, [None, 2, 'nearest']] # 22
  - [13, 1, Conv, [256, 1, 1, None, 1, 1, False]] # 23
  - [[-2, -1], 1, Concat, [1]] # 24
  - [-1, 3, RepC3, [256, 0.5]] # 25

  - [-1, 1, Conv, [256, 3, 2]] # 26
  - [[-1, 21], 1, Concat, [1]] # 27
  - [-1, 3, RepC3, [256, 0.5]] # 28

  - [-1, 1, Conv, [256, 3, 2]] # 29
  - [[-1, 16], 1, Concat, [1]] # 30
  - [-1, 3, RepC3, [256, 0.5]] # 31

  - [[25, 28, 31], 1, RTDETRDecoder, [nc, 256, 300, 4, 8, 3]] # 32
